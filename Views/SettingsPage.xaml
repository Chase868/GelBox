<?xml version="1.0" encoding="utf-8"?>

<views:BasePage
    x:Class="GelBox.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:GelBox.Views"
    xmlns:viewmodels="using:GelBox.ViewModels"
    mc:Ignorable="d"
    Background="{StaticResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0"
              Height="64">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="&#xE72B;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="16"
                    Click="BackButton_Click"
                    Background="Transparent"
                    VerticalAlignment="Center"
                    Margin="48,0,0,0"
                    Padding="8"
                    ToolTipService.ToolTip="Back" />
            <TextBlock Grid.Column="1"
                       Text="Settings"
                       Style="{StaticResource TitleTextStyle}"
                       Margin="12,0,48,0"
                       VerticalAlignment="Center" />
        </Grid>

        <ScrollViewer Grid.Row="1" Padding="{StaticResource SafeAreaPadding}">
            <StackPanel Style="{StaticResource StandardPageContainerStyle}"
                        MaxWidth="720">

                <!-- Server Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="ServerSettings">
                        <TextBlock Text="Server" Style="{StaticResource SectionHeaderTextStyle}" />

                    <Button Content="Sign Out"
                            Command="{x:Bind TypedViewModel.ServerSettings.SignOutCommand}"
                            Style="{StaticResource SecondaryButtonStyle}" />

                    <TextBlock Text="Connection" Style="{StaticResource SubsectionHeaderTextStyle}"
                               Margin="{StaticResource HeaderMargin}" />
                    <StackPanel>
                        <TextBlock>
                            <Run Text="Connection Timeout: " />
                            <Run Text="{x:Bind TypedViewModel.ServerSettings.ConnectionTimeout, Mode=OneWay}" />
                            <Run Text=" seconds" />
                        </TextBlock>
                        <Slider Minimum="5"
                                Maximum="120"
                                StepFrequency="5"
                                Value="{x:Bind TypedViewModel.ServerSettings.ConnectionTimeout, Mode=TwoWay}"
                                Style="{StaticResource StandardSliderStyle}" />
                        <TextBlock Text="Changes to connection timeout require app restart to take effect."
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   Opacity="0.7"
                                   Margin="0,4,0,0"
                                   TextWrapping="Wrap"
                                   FontSize="12" />
                    </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Accessibility Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="AccessibilitySettings">
                        <TextBlock Text="Accessibility" Style="{StaticResource SectionHeaderTextStyle}" />
                    <StackPanel>
                        <TextBlock>
                            <Run Text="Text Size: " />
                            <Run Text="{x:Bind TypedViewModel.TextSize, Mode=OneWay, FallbackValue=14}" />
                            <Run Text="pt" />
                        </TextBlock>
                        <Slider Minimum="12" Maximum="24"
                                StepFrequency="1"
                                Value="{x:Bind TypedViewModel.TextSize, Mode=TwoWay}"
                                Style="{StaticResource StandardSliderStyle}" />
                    </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Audio Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="AudioSettings">
                        <TextBlock Text="Audio" Style="{StaticResource SectionHeaderTextStyle}" />

                    <ToggleSwitch Header="Night Mode"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.IsNightModeEnabled, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}" />
                    <TextBlock
                        Text="Reduces the difference between loud and quiet sounds. Perfect for late-night viewing when you don't want explosions to wake others but still need to hear dialogue clearly."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />
                    </StackPanel>
                </Border>

                <!-- Video Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="VideoSettings">
                        <TextBlock Text="Video" Style="{StaticResource SectionHeaderTextStyle}" />

                    <ComboBox Header="Display Mode"
                              x:Name="VideoStretchModeComboBox"
                              SelectedValuePath="Tag"
                              SelectedValue="{x:Bind TypedViewModel.PlaybackSettings.VideoStretchMode, Mode=TwoWay}"
                              Style="{StaticResource StandardComboBoxStyle}">
                        <ComboBoxItem Content="Original Aspect Ratio" Tag="Uniform" />
                        <ComboBoxItem Content="Fill Screen (Zoom)" Tag="UniformToFill" />
                    </ComboBox>
                    <TextBlock
                        Text="Original Aspect Ratio: Shows the entire video with black bars if needed. Fill Screen: Zooms in to fill your display, may crop edges."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />
                    </StackPanel>
                </Border>

                <!-- Appearance Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="AppearanceSettings">
                        <TextBlock Text="Appearance" Style="{StaticResource SectionHeaderTextStyle}" />

                    <ToggleSwitch Header="Gradient Background"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.EnableGradientBackground, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}"
                                  OnContent="Green-to-blue gradient"
                                  OffContent="Solid dark background" />
                    <TextBlock
                        Text="Adds a subtle green-to-blue gradient background to all pages, matching the app logo colors. Disable for a plain dark background."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />
                    </StackPanel>
                </Border>

                <!-- Home Screen Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="HomeScreenSettings">
                        <TextBlock Text="Home Screen" Style="{StaticResource SectionHeaderTextStyle}" />

                    <ToggleSwitch Header="Show Movies"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.ShowMoviesOnHome, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}" />
                    <TextBlock
                        Text="Show the Latest Movies section on the home screen."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Show TV Shows"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.ShowTVShowsOnHome, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}" />
                    <TextBlock
                        Text="Show the Latest TV Shows section on the home screen."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Show Music"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.ShowMusicOnHome, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}" />
                    <TextBlock
                        Text="Show the Latest Music section on the home screen."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <TextBlock
                        Text="Changes take effect after refreshing the home screen."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.5"
                        Margin="0,4,0,0"
                        TextWrapping="Wrap"
                        FontSize="12"
                        FontStyle="Italic" />
                    </StackPanel>
                </Border>

                <!-- Playback Settings -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        CornerRadius="{StaticResource MediumCornerRadius}"
                        Padding="20"
                        Margin="0,0,0,8">
                    <StackPanel x:Name="PlaybackSettings">
                        <TextBlock Text="Playback" Style="{StaticResource SectionHeaderTextStyle}" />


                    <ToggleSwitch Header="Auto-Play Next Episode"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.AutoPlayNextEpisode, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}" />
                    <TextBlock
                        Text="Automatically starts the next episode when the current one ends. Great for binge-watching TV shows."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Pause When Xbox Guide Opens"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.PauseOnFocusLoss, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}" />
                    <TextBlock
                        Text="Automatically pauses playback when you open the Xbox Guide or switch apps. Prevents missing content while multitasking."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Auto-Skip Intros"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.AutoSkipIntros, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}"
                                  OnContent="Skip intros automatically"
                                  OffContent="Show intro skip button" />
                    <TextBlock
                        Text="When enabled, automatically skips TV show intros. When disabled, shows a button to skip manually. Requires server-side intro detection."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <StackPanel>
                        <TextBlock>
                            <Run Text="Controls Auto-Hide Delay: " />
                            <Run Text="{x:Bind TypedViewModel.PlaybackSettings.ControlsHideDelay, Mode=OneWay}" />
                            <Run Text=" seconds" />
                        </TextBlock>
                        <Slider Minimum="1"
                                Maximum="30"
                                Value="{x:Bind TypedViewModel.PlaybackSettings.ControlsHideDelay, Mode=TwoWay}"
                                StepFrequency="1"
                                TickFrequency="5"
                                SnapsTo="StepValues"
                                Style="{StaticResource StandardSliderStyle}" />
                    </StackPanel>


                    <ToggleSwitch Header="Direct Play"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.EnableDirectPlay, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}"
                                  OnContent="Stream original files when compatible"
                                  OffContent="Always convert video for compatibility" />
                    <TextBlock
                        Text="When enabled, plays media files directly without conversion if your device supports the format. Provides best quality and uses less server resources. Disable if you experience playback issues."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Audio Direct Stream"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.AllowAudioStreamCopy, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}"
                                  OnContent="Stream original audio without conversion"
                                  OffContent="Allow audio conversion for compatibility" />
                    <TextBlock
                        Text="Streams audio in its original format when possible. Provides best audio quality but may not work with all audio formats. Disable if you have no sound or audio sync issues."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Volume Normalization"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.EnableVolumeNormalization, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}"
                                  OnContent="Normalize volume using LUFS data"
                                  OffContent="Play at original volume levels" />
                    <TextBlock
                        Text="Automatically adjusts volume levels to compensate for differences between tracks. Uses Loudness Units Full Scale (LUFS) data calculated by the Jellyfin server."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <ToggleSwitch Header="Album Gain Mode"
                                  IsOn="{x:Bind TypedViewModel.PlaybackSettings.UseAlbumGain, Mode=TwoWay}"
                                  Style="{StaticResource StandardToggleSwitchStyle}"
                                  OnContent="Use album-level normalization"
                                  OffContent="Use track-level normalization" />
                    <TextBlock
                        Text="When enabled, uses the same volume adjustment for all tracks in an album. When disabled, adjusts each track individually. Requires Volume Normalization to be enabled."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    <StackPanel>
                        <TextBlock>
                            <Run Text="LUFS Target Level: " />
                            <Run Text="{x:Bind TypedViewModel.PlaybackSettings.LufsTarget, Mode=OneWay}" />
                            <Run Text=" LUFS" />
                        </TextBlock>
                        <Slider Minimum="-30"
                                Maximum="-10"
                                Value="{x:Bind TypedViewModel.PlaybackSettings.LufsTarget, Mode=TwoWay}"
                                StepFrequency="0.5"
                                TickFrequency="2"
                                SnapsTo="StepValues"
                                Style="{StaticResource StandardSliderStyle}" />
                    </StackPanel>
                    <TextBlock
                        Text="Adjusts the target loudness level for volume normalization. Lower values (-23 to -30) provide quieter, more consistent playback. Higher values (-16 to -10) allow louder playback but with more variation between tracks."
                        Style="{StaticResource BodyTextBlockStyle}"
                        Opacity="0.7"
                        Margin="0,0,0,12"
                        TextWrapping="Wrap"
                        FontSize="12" />

                    </StackPanel>
                </Border>

                <!-- About Section -->
                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="About"
                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                   Margin="0,0,0,12" />

                        <TextBlock Text="GelBox v1.0.0"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   Margin="0,0,0,8" />

                        <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                                   Opacity="0.7"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12"
                                   FontSize="13">
                            A Jellyfin client for Xbox, forked from Gelatinarm.
                        </TextBlock>

                        <HyperlinkButton NavigateUri="https://github.com/Chase868/GelBox"
                                         Content="GelBox on GitHub"
                                         Padding="0"
                                         Margin="0,0,0,4" />

                        <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                                   Opacity="0.7"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,4"
                                   FontSize="13">
                            Based on the original Gelatinarm app:
                        </TextBlock>

                        <HyperlinkButton NavigateUri="https://github.com/gitman-101111/gelatinarm"
                                         Content="Gelatinarm on GitHub"
                                         Padding="0"
                                         Margin="0,0,0,8" />

                        <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                                   Opacity="0.5"
                                   FontSize="12"
                                   Margin="0,4,0,0">
                            Licensed under the MIT License.
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</views:BasePage>
